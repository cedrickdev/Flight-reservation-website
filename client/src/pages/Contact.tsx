/** Direction L'Itinéraire d'Or: the contact page is a quiet conversion space focused on one considered enquiry. */
import { Mail,MapPin,MessageCircle } from "lucide-react";import { RequestStepper } from "@/components/RequestStepper";import { SEO } from "@/components/SEO";import { contact } from "@/lib/content";import { useLanguage } from "@/contexts/LanguageContext";
export default function Contact(){const {language}=useLanguage();const isFr=language==="fr";return <><SEO title={isFr?"Nous contacter":"Contact us"} path="/contact"/><main><section className="contact-hero section-shell"><div className="section-marker"><span>01</span><span>{isFr?"Prendre contact":"Get in touch"}</span></div><div className="contact-heading"><h1>{isFr?<>Votre prochaine étape,<br/><em>en quelques questions.</em></>:<>Your next step,<br/><em>in a few questions.</em></>}</h1><p>{isFr?"Décrivez simplement votre besoin. Le formulaire est conçu pour nous donner le bon point de départ.":"Simply describe what you need. The form is designed to give us the right starting point."}</p></div></section><section className="contact-area"><div className="section-shell contact-grid"><RequestStepper/><aside className="contact-aside"><p className="service-eyebrow">{isFr?"Nous joindre directement":"Reach us directly"}</p><div className="contact-item"><MessageCircle size={19}/><div><span>WhatsApp</span>{contact.phones.map(phone=><a key={phone.wa} href={`https://wa.me/${phone.wa}`} target="_blank" rel="noreferrer">{phone.display}</a>)}</div></div><div className="contact-item"><Mail size={19}/><div><span>E-mail</span><a href={`mailto:${contact.email}`}>{contact.email}</a></div></div><div className="contact-item"><MapPin size={19}/><div><span>{isFr?"Base":"Base"}</span><strong>{contact.city[language]}</strong></div></div><p className="aside-note">{isFr?"L’adresse e-mail utilisée ici est un emplacement provisoire, prêt à être remplacé dès que votre boîte professionnelle est confirmée.":"The email address shown here is a temporary placeholder, ready to be replaced once your professional inbox is confirmed."}</p></aside></div></section></main></>}
