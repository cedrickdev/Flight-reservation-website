/** Direction L'Itinéraire d'Or: each service page makes one offering feel editorial, grounded and actionable. */
import { ArrowLeft,ArrowUpRight,Check,Mail } from "lucide-react";import { Link,useRoute } from "wouter";import { SEO } from "@/components/SEO";import { assets,services,tr,ui } from "@/lib/content";import { useLanguage } from "@/contexts/LanguageContext";
export default function ServiceDetail(){const [,params]=useRoute("/services/:slug");const {language}=useLanguage();const service=services.find(item=>item.slug===params?.slug)||services[0];const isFr=language==="fr";const image=service.image;return <><SEO title={tr(service.name,language)} path={`/services/${service.slug}`}/><main><section className={image?"detail-hero has-image":"detail-hero"}>{image?<img src={image} alt="" className="detail-hero-image" width="1200" height="1200"/>:<div className="detail-abstract"><span>{service.number}</span><img src={assets.mark} alt="" width="240" height="240"/></div>}<div className="detail-overlay"/><div className="detail-content section-shell"><Link href="/services" className="back-link"><ArrowLeft size={16}/>{isFr?"Tous les services":"All services"}</Link><p className="hero-pretitle"><span className="gold-dot"/>{tr(service.eyebrow,language)}</p><h1>{tr(service.name,language)}</h1><p className="detail-lead">{tr(service.summary,language)}</p><Link href="/contact" className="button-primary">{ui[language].request}<ArrowUpRight size={18}/></Link></div></section><section className="detail-body section-shell"><div className="section-marker"><span>{service.number}</span><span>{isFr?"Notre accompagnement":"Our support"}</span></div><div className="detail-copy-grid"><div><h2>{isFr?<>Avancer avec<br/><em>plus de clarté.</em></>:<>Move forward<br/><em>with more clarity.</em></>}</h2></div><p>{tr(service.longDescription,language)}</p></div><div className="deliverables"><p className="service-eyebrow">{isFr?"Ce que nous préparons avec vous":"What we prepare with you"}</p>{service.deliverables.map((item,index)=><div key={item.fr} className="deliverable"><span>0{index+1}</span><p>{tr(item,language)}</p><Check size={19}/></div>)}</div></section><section className="detail-next"><div className="section-shell detail-next-inner"><div><p className="service-eyebrow">{isFr?"Une demande simple":"A simple enquiry"}</p><h2>{isFr?<>Décrivez votre besoin.<br/><em>Nous préparons la suite.</em></>:<>Tell us what you need.<br/><em>We will prepare what follows.</em></>}</h2></div><Link href="/contact" className="button-light"><Mail size={17}/>{ui[language].contact}</Link></div></section></main></>}
