import { ChangeEvent, FormEvent, useMemo, useState } from "react";
import { ArrowLeft, ArrowRight, Check, FileUp, LoaderCircle, PlaneTakeoff, X } from "lucide-react";
import { services, tr } from "@/lib/content";
import { trpc } from "@/lib/trpc";
import { useLanguage } from "@/contexts/LanguageContext";

type RequestData = { service: string; project: string; destination: string; departureDate: string; returnDate: string; travelers: string; fullName: string; phone: string; email: string; details: string; files: File[] };
const initialData: RequestData = { service: "", project: "", destination: "", departureDate: "", returnDate: "", travelers: "1", fullName: "", phone: "", email: "", details: "", files: [] };
const allowedFileTypes = ["application/pdf", "image/jpeg", "image/png"];
const maxFileSize = 5 * 1024 * 1024;

function fileAsBase64(file: File) { return new Promise<string>((resolve, reject) => { const reader = new FileReader(); reader.onload = () => resolve(String(reader.result).split(",")[1] || ""); reader.onerror = () => reject(reader.error); reader.readAsDataURL(file); }); }

export function RequestStepper() {
  const { language } = useLanguage();
  const [step, setStep] = useState(0);
  const [data, setData] = useState<RequestData>(initialData);
  const [submitted, setSubmitted] = useState(false);
  const [fileError, setFileError] = useState("");
  const labels = language === "fr" ? ["Votre besoin", "Votre voyage", "Vos coordonnées", "Confirmation"] : ["Your need", "Your trip", "Your details", "Confirmation"];
  const createRequest = trpc.requests.create.useMutation({ onSuccess: () => setSubmitted(true) });
  const valid = useMemo(() => step === 0 ? Boolean(data.service) : step === 1 ? Boolean(data.project && data.destination && data.departureDate && data.travelers) : step === 2 ? Boolean(data.fullName && data.phone && data.email) : true, [data, step]);
  const update = (field: Exclude<keyof RequestData, "files">, value: string) => setData(current => ({ ...current, [field]: value }));

  const addFiles = (event: ChangeEvent<HTMLInputElement>) => {
    const incoming = Array.from(event.target.files || []);
    const proposed = [...data.files, ...incoming].slice(0, 3);
    const invalid = incoming.find(file => !allowedFileTypes.includes(file.type) || file.size > maxFileSize);
    setFileError(invalid ? (language === "fr" ? "Formats acceptés : PDF, JPG ou PNG, 5 Mo maximum par document." : "Accepted formats: PDF, JPG or PNG, up to 5 MB per document.") : "");
    if (!invalid) setData(current => ({ ...current, files: proposed }));
    event.target.value = "";
  };

  const submit = async (event: FormEvent) => {
    event.preventDefault();
    const attachments = await Promise.all(data.files.map(async file => ({ name: file.name, mimeType: file.type, sizeBytes: file.size, dataBase64: await fileAsBase64(file) })));
    const travelDetails = [
      data.details,
      `${language === "fr" ? "Destination" : "Destination"}: ${data.destination}`,
      `${language === "fr" ? "Départ" : "Departure"}: ${data.departureDate}`,
      `${language === "fr" ? "Retour" : "Return"}: ${data.returnDate || "—"}`,
      `${language === "fr" ? "Voyageurs" : "Travelers"}: ${data.travelers}`,
    ].filter(Boolean).join("\n");
    createRequest.mutate({ service: data.service, project: data.project, requesterName: data.fullName, phone: data.phone, email: data.email, details: travelDetails || undefined, attachments });
  };

  if (submitted) return <div className="form-complete" role="status" aria-live="polite"><div className="complete-icon" aria-hidden="true"><span className="complete-icon-pulse" /><Check size={22} /></div><p className="service-eyebrow">{language === "fr" ? "Demande envoyée" : "Request sent"}</p><h3>{language === "fr" ? "Votre demande est bien reçue." : "We received your request."}</h3><p>{language === "fr" ? "Merci. Notre équipe traite votre demande et reviendra vers vous sous peu à l’adresse e-mail indiquée." : "Thank you. Our team is reviewing your request and will get back to you shortly at the email address you provided."}</p><p className="confirmation-note">{language === "fr" ? "Un e-mail de confirmation vient de vous être envoyé." : "A confirmation email has just been sent to you."}</p></div>;

  return <form className="request-stepper" onSubmit={submit}><ol className="stepper-progress" aria-label={language === "fr" ? "Étapes du formulaire" : "Form steps"}>{labels.map((label, index) => <li key={label} className={index <= step ? "is-active" : ""}><span>{index < step ? <Check size={13} /> : `0${index + 1}`}</span><em>{label}</em></li>)}</ol>
    {step === 0 && <fieldset className="step-panel"><legend>{language === "fr" ? "Quel accompagnement recherchez-vous ?" : "Which service are you looking for?"}</legend><p>{language === "fr" ? "Choisissez le point de départ de votre demande." : "Choose the starting point of your enquiry."}</p><div className="service-choice-grid">{services.map(service => <label key={service.slug} className={data.service === tr(service.name, language) ? "service-choice selected" : "service-choice"}><input type="radio" name="service" value={tr(service.name, language)} checked={data.service === tr(service.name, language)} onChange={event => update("service", event.target.value)} /><span>{service.number}</span>{tr(service.name, language)}</label>)}</div></fieldset>}
    {step === 1 && <fieldset className="step-panel"><legend>{language === "fr" ? "Parlez-nous de votre voyage." : "Tell us about your trip."}</legend><p>{language === "fr" ? "Ces informations nous permettent de préparer un premier retour utile." : "These details help us prepare a useful first response."}</p><label className="field-label" htmlFor="project">{language === "fr" ? "Objet de votre demande" : "Request subject"}</label><input id="project" required value={data.project} onChange={event => update("project", event.target.value)} placeholder={language === "fr" ? "Ex. Voyage familial, visa étudiant…" : "E.g. Family trip, student visa…"} /><label className="field-label" htmlFor="destination">{language === "fr" ? "Destination" : "Destination"}</label><input id="destination" required value={data.destination} onChange={event => update("destination", event.target.value)} placeholder={language === "fr" ? "Ex. Paris, Istanbul, Yaoundé…" : "E.g. Paris, Istanbul, Yaoundé…"} /><div className="form-two-columns"><div><label className="field-label" htmlFor="departureDate">{language === "fr" ? "Date de départ" : "Departure date"}</label><input id="departureDate" required type="date" value={data.departureDate} onChange={event => update("departureDate", event.target.value)} /></div><div><label className="field-label" htmlFor="returnDate">{language === "fr" ? "Date de retour" : "Return date"}</label><input id="returnDate" type="date" value={data.returnDate} onChange={event => update("returnDate", event.target.value)} /></div></div><label className="field-label" htmlFor="travelers">{language === "fr" ? "Nombre de voyageurs" : "Number of travelers"}</label><input id="travelers" required type="number" min="1" max="30" inputMode="numeric" value={data.travelers} onChange={event => update("travelers", event.target.value)} /><label className="field-label" htmlFor="details">{language === "fr" ? "Précisions utiles (facultatif)" : "Useful details (optional)"}</label><textarea id="details" rows={3} value={data.details} onChange={event => update("details", event.target.value)} placeholder={language === "fr" ? "Contexte, préférences, contraintes…" : "Context, preferences, constraints…"} /><div className="document-upload"><div><FileUp size={18} aria-hidden="true" /><span><strong>{language === "fr" ? "Joindre des documents" : "Attach documents"}</strong><small>{language === "fr" ? "Facultatif · PDF, JPG ou PNG · 3 fichiers maximum · 5 Mo par fichier" : "Optional · PDF, JPG or PNG · 3 files maximum · 5 MB per file"}</small></span></div><label className="upload-trigger"><input type="file" accept=".pdf,.jpg,.jpeg,.png,application/pdf,image/jpeg,image/png" multiple onChange={addFiles} />{language === "fr" ? "Ajouter" : "Add"}</label></div>{fileError && <p className="file-error" id="file-error" role="alert">{fileError}</p>}{data.files.length > 0 && <div className="file-list">{data.files.map(file => <div key={`${file.name}-${file.lastModified}`}><span>{file.name}</span><button type="button" onClick={() => setData(current => ({ ...current, files: current.files.filter(item => item !== file) }))} aria-label={language === "fr" ? `Retirer ${file.name}` : `Remove ${file.name}`}><X size={14} aria-hidden="true" /></button></div>)}</div>}</fieldset>}
    {step === 2 && <fieldset className="step-panel"><legend>{language === "fr" ? "Comment pouvons-nous vous répondre ?" : "How can we reply to you?"}</legend><p>{language === "fr" ? "Nous utiliserons ces informations uniquement pour traiter votre demande." : "We will use these details only to handle your enquiry."}</p><div className="form-two-columns"><div><label className="field-label" htmlFor="fullname">{language === "fr" ? "Nom complet" : "Full name"}</label><input id="fullname" required value={data.fullName} onChange={event => update("fullName", event.target.value)} /></div><div><label className="field-label" htmlFor="phone">{language === "fr" ? "Numéro WhatsApp" : "WhatsApp number"}</label><input id="phone" required inputMode="tel" value={data.phone} onChange={event => update("phone", event.target.value)} placeholder="+237 …" /></div></div><label className="field-label" htmlFor="email">E-mail</label><input id="email" type="email" required value={data.email} onChange={event => update("email", event.target.value)} /></fieldset>}
    {step === 3 && <fieldset className="step-panel confirm-panel"><div className="confirm-icon"><PlaneTakeoff size={24} /></div><legend>{language === "fr" ? "Votre demande est prête." : "Your request is ready."}</legend><p>{language === "fr" ? "Vérifiez les éléments ci-dessous. Votre demande et vos éventuels documents seront transmis de manière sécurisée." : "Review the details below. Your request and optional documents will be transmitted securely."}</p><dl><div><dt>Service</dt><dd>{data.service}</dd></div><div><dt>{language === "fr" ? "Destination" : "Destination"}</dt><dd>{data.destination}</dd></div><div><dt>{language === "fr" ? "Dates" : "Dates"}</dt><dd>{data.departureDate}{data.returnDate ? ` → ${data.returnDate}` : ""}</dd></div><div><dt>{language === "fr" ? "Voyageurs" : "Travelers"}</dt><dd>{data.travelers}</dd></div><div><dt>Contact</dt><dd>{data.fullName} · {data.email}</dd></div></dl>{createRequest.error && <p className="file-error" role="alert">{language === "fr" ? "La demande n’a pas pu être envoyée. Veuillez réessayer." : "The request could not be sent. Please try again."}</p>}</fieldset>}
    <div className="stepper-actions">{step > 0 ? <button type="button" className="button-secondary" onClick={() => setStep(current => current - 1)}><ArrowLeft size={17} />{language === "fr" ? "Retour" : "Back"}</button> : <span />}{step < 3 ? <button type="button" disabled={!valid} className="button-primary" onClick={() => setStep(current => current + 1)}>{language === "fr" ? "Continuer" : "Continue"}<ArrowRight size={17} /></button> : <button className="button-primary" type="submit" disabled={createRequest.isPending}>{createRequest.isPending ? <LoaderCircle className="animate-spin" size={17} /> : <Check size={17} />} {language === "fr" ? "Envoyer ma demande" : "Send my request"}</button>}</div></form>;
}
