"use client";

/** Direction L'Itinéraire d'Or: service cards feel like successive itinerary stops rather than generic product tiles. */
import Link from "next/link";import { ArrowUpRight,BriefcaseBusiness,ConciergeBell,FileCheck2,Hotel,Package,Plane,ShieldCheck,Sparkles } from "lucide-react";import type { TravelService } from "@/lib/content";import { tr,ui } from "@/lib/content";import { useLanguage } from "@/contexts/LanguageContext";
const icons={visa:FileCheck2,billets:Plane,hotels:Hotel,packages:Package,assurance:ShieldCheck,conciergerie:ConciergeBell,coaching:BriefcaseBusiness};
export function ServiceCard({service,featured=false}:{service:TravelService;featured?:boolean}){const {language}=useLanguage();const Icon=icons[service.slug]||Sparkles;return <article className={featured?"service-card service-card-featured":"service-card"}><div className="service-card-top"><span className="service-number">{service.number}</span><Icon className="service-icon" size={24} strokeWidth={1.35}/></div><div><p className="service-eyebrow">{tr(service.eyebrow,language)}</p><h3>{tr(service.name,language)}</h3><p className="service-summary">{tr(service.summary,language)}</p></div><Link href={`/services/${service.slug}`} className="service-link"><span>{ui[language].discover}</span><ArrowUpRight size={18} strokeWidth={1.6}/></Link></article>}
