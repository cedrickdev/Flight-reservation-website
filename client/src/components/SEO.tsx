/** Direction L'Itinéraire d'Or: concise metadata gives each page a clear, trustworthy destination. */
import { useEffect } from "react";
import { useLanguage } from "@/contexts/LanguageContext";
const defaultDescription={fr:"Trust Elite Travel accompagne vos projets de voyage : assistance visa, billets d’avion, hôtels, assurance voyage et coaching hôtesses de l’air.",en:"Trust Elite Travel supports travel plans with visa assistance, air tickets, hotel reservations, travel insurance and cabin crew coaching."};
export function SEO({title,description,path=""}:{title:string;description?:string;path?:string}) { const {language}=useLanguage(); useEffect(()=>{const content=description||defaultDescription[language];const fullTitle=`${title} | Trust Elite Travel`;document.title=fullTitle;const setMeta=(selector:string,attribute:"name"|"property",key:string,value:string)=>{let node=document.head.querySelector<HTMLMetaElement>(selector);if(!node){node=document.createElement("meta");node.setAttribute(attribute,key);document.head.appendChild(node)}node.content=value};setMeta('meta[name="description"]',"name","description",content);setMeta('meta[property="og:title"]',"property","og:title",fullTitle);setMeta('meta[property="og:description"]',"property","og:description",content);let canonical=document.head.querySelector<HTMLLinkElement>('link[rel="canonical"]');if(!canonical){canonical=document.createElement("link");canonical.rel="canonical";document.head.appendChild(canonical)}canonical.href=`https://trust-elite-travels.com${path}`},[description,language,path,title]);return null; }
